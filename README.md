## 🚀 Реальные расстояния по дорогам (OSRM)

Для точных маршрутов используется локальный сервер OSRM.

### Как запустить:
1. Убедитесь, что Docker Desktop работает.
2. Запустите файл `start_osrm.bat` (в корне проекта).
3. Оставьте окно открытым.
4. Запустите проект:
   ```bash
   python src/main.py --data data/data.csv